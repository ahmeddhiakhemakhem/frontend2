import axios from "axios";
export default axios.create({
    baseURL: 'https://ecommercebackend-ebon.vercel.app/api/', // API server URL
     // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)  // Add headers for API request (if needed)
})